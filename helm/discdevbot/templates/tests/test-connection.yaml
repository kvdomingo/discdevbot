apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "discdevbot.fullname" . }}-test-connection"
  labels:
    {{- include "discdevbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "discdevbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
